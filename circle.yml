machine:
  java:
    version: openjdk8

test:
  override:
    - sudo mkdir -p /var/log/ks-enricher/ && sudo chown -R ubuntu:ubuntu /var/log/ks-enricher/
    - mvn clean test
  post:
    - mvn package -Dmaven.test.skip=true -P dist
    - cp distribution/target/enricher-dist.tar.gz $CIRCLE_ARTIFACTS/