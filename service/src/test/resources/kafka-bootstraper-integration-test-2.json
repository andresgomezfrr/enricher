{
  "joiners": [
    {"name":"streamPreferred", "className":"io.wizzie.ks.enrichment.join.impl.StreamPreferredJoiner"}
  ],
  "queries": {
    "query1": "SELECT a FROM STREAM stream1 JOIN SELECT * FROM TABLE table1 USING streamPreferred JOIN SELECT j FROM TABLE table2 USING streamPreferred INSERT INTO TABLE output"
  }
}